version: "3.8"

services:
  dashboard:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: powerlifting_dashboard
    ports:
      - "${STREAMLIT_PORT}:8501"
    volumes:
      - ../data:/app/data
    env_file:
      - .env
    restart: unless-stopped
